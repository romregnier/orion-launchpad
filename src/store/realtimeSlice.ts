/**
 * realtimeSlice.ts — Placeholder for future realtime-specific state.
 * Currently, realtime subscriptions are co-located with their domain slices
 * (subscribeToProjects in canvasSlice, subscribeToAgents in agentsSlice, etc.)
 * This slice ensures the structure is in place for future extraction.
 */

// No additional state — all realtime logic lives in domain slices.
// Export an empty creator to satisfy the slice file requirement.
export {}
